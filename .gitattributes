# Track large media via Git LFS

# Track all files in tests/data via Git LFS
tests/data/** filter=lfs diff=lfs merge=lfs -text

# Specific large video for testing
tests/data/test_video_30s.mp4 filter=lfs diff=lfs merge=lfs -text
